/**
 * @format
 */

import {AppRegistry} from 'react-native';
import XebiaApplication from './src/components/Mainroutes/XebiaApplication';
import {name as appName} from './app.json';

AppRegistry.registerComponent(appName, () => XebiaApplication);
